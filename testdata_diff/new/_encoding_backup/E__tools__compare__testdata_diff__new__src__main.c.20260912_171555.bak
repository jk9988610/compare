#include "main.h"
#include <stdio.h>

// 新版本主循环：增加初始化
int main(void)
{
    int counter = 0;
    init_board();
    while (1) {
        counter++;
        sleep_ms(10);
        kick_watchdog();
    }
    return 0;
}
